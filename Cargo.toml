[package]
name = "conllu"
version = "0.1.0"
edition = "2024"

[dependencies]
ordermap = "1.0.0"
regex = "1.12.2"

[dev-dependencies]
eyre = "0.6.12"
insta = { version = "1.43.2"}
# , features = ["json"] }
rstest = "0.26.1"
# serde = { version = "1.0.228", features = ["derive"] }
